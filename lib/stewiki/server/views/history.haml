.contentarea
  %h1= @title
  - @page.commits.each do |commit|
    %h3 #{commit.authored_date}
    %p
      Commit #{commit.id_abbrev}, by
      %a{:href => "mailto:#{commit.author.email}"}= commit.author.name
    %ul
      %li= commit.message
